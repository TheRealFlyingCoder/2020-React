import { css } from '@emotion/core';
import Theme from 'Theme';

export default css`
	width: 100%;
	background-color: ${Theme.colours.secondary};
	color: white;
	font-size: 12px;
	text-align: center;
`;
