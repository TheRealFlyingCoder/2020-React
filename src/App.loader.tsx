import React from 'react';
import { css } from '@emotion/core';

const styles = css``;

//Full Page Loader for async startup
const App: React.FC = () => (
	<div css={styles}>
		...Loading
	</div>
);

export default App;
